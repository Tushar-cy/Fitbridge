// This file is kept to avoid import errors in other branches or files.
// The old JWT utility functions (generateAccessToken, verifyAccessToken, generateRefreshToken)
// have been removed because we now use Supabase JWT verification.
export {};
